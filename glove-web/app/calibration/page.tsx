import CalibrationMode from "@/components/CalibrationMode";

export const metadata = { title: "Flex Calibration — Ghost Glove" };

export default function CalibrationPage() {
  return <CalibrationMode />;
}
